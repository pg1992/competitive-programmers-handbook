#include <bits/stdc++.h>

typedef long long ll;
typedef vector<int> vi;
typedef pair<int, int> pi;

using namespace std;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	freopen("input.txt", "r", stdin);
	freopen("output.txt", "w", stdout);
	int a, b;
	string x;
	cin >> a >> b >> x >> ws;
	cout << a << " " << b << ' ' << x << endl;

	string s;
	getline(cin, s);
	printf("s = \"%s\"\n", s.c_str());

	// Defining a long long
	ll xll = 123456789123456789LL;
}
